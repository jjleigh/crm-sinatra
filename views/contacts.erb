
<div id="see-contacts">
	<h2>Contacts</h2>
	<ul>
		<% @@rolodex.contacts.each do |contact| %>
			<li>
				<%= contact.first_name %><%= contact.last_name %>
				<a href="/contacts/:id<%= contact.id %>">Details</a>
			</li>
		<% end %>
	</ul>
</div>
